<h4>Post details:</h4>
<br />
<p>
	<%= @row['content'] %>
		<br />
		<br />
		Added at <%= @row['created_date'] %><!--выводим дату создания-->
		
</p>	
<h4>Comments:</h4> <!--выведем форму с комментариями-->
<br />
<!--выводим наши комментарии-->
<% @comments.each do |comment| %>
	<p>
		<%= comment['content'] %>
		<br />
		<br />
		<span style="font-size: 75%; color: #999">Added at <%= comment['created_date'] %></span><!--шрифтом поменьше span-->
	</p>
	<hr />
<% %>
<p><!--копируем форму с bootstrap ->css (добавляем комментарий)-->
	<form action="/details/<%= @row['id'] %>" method="POST" role="form" > <!--action будет динамически меняться для каждой страницы(появился атрибут который будет динамически генерироваться)--><!--обязательные атрибуты form, которые надо запомнить и которые должны быть у формы: 1. атрибут action - говорит о том куда будут отправляться данные, а нашем случае по url /new, 2.атрибут method может принимать  GET и POST запросы(в нашем случае POST - отправка данных) !!! Важно запомнить, часто встречается-->
	  <textarea name="content" class="form-control" rows="6" placeholder="Type comment text here" style="width: 400px"></textarea> <!--теперь добавим Textaria - отвечает за ввод данных на многих линиях. Интересный атрибут placeholder(появляется серым - введите здесь...)-  фишка html. style="width: 400px" - добавляем CSS стиль(изменяем ширину поля). name - имя с помощью которого мы сможем обратиться к textarea -->
	  <br />  <!--отодвинем кнопку-->
	  <button type="submit" class="btn btn-primary">Submit</button> <!--сделаем кнопку синей, btn-primary вместо btn-default-->
	</form>
</p>
